# FiveM fixtures

These synthetic JSON documents represent successful responses from FiveM's fixed `info.json`, `dynamic.json`, and `players.json` sources. Together they cover software identity, resources, variables, normalized server fields, and the public player list.

The values are documentation-only examples and contain no live-server or personal data.
