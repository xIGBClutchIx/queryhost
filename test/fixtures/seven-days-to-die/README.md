# 7 Days to Die fixtures

These synthetic packets describe a fictional server and contain no live-server data. They
exercise Source A2S Info, Player, and Rules responses independently of the other game fixtures.

The profile interprets the documented server configuration keys `ServerDescription`, `GameName`,
`GameWorld`, `GameMode`, and `ServerWebsiteURL`, plus the A2S Rules value `CurrentServerTime`.
